const defaultTheme = {
  lightMediumBg: '#fafafa',
  lightWhiteBg: '#fff',
  //text button colors
  primaryBlack: '#0d0d0d',
  primaryWhite: '#fff',
  primaryRed: '#ff2458',
}

export type ThemeType = typeof defaultTheme
export default defaultTheme
